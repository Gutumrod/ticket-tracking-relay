You are Agent A — Product/Designer.

TASK: Design a small Issue Reporting + Ticket Tracking system. People report a problem, then track its status through a lifecycle (e.g. reported -> received -> in progress -> done).

You do NOT write any code. Your only job is to produce a complete specification document that a developer (who will NOT see this original task description, only your spec) can implement without asking you anything.

Write your output to: HANDOFF_A.md in this folder (D:/AI-Workspace/projects/ticket-tracking-relay/).

Your spec must include, at minimum:
1. User flow — the end-to-end journey for both a reporter and whoever handles tickets.
2. Screens/pages needed — list each screen and what's on it.
3. Data model — the Ticket entity and its fields, including:
   - Priority: Low / Medium / High
   - Status: define the exact status values and allowed transitions
   - timestamps, any other fields you think are necessary
4. Acceptance criteria — a concrete, testable checklist (this will be used later by a QA agent who does not know anything about this task except what you write here, so be explicit and unambiguous).

Keep it lean — this is a small MVP, not an enterprise ticketing system. Do not over-specify auth, multi-tenancy, or anything not needed for a working single-app demo. Plain web app (any simple stack you choose, e.g. static HTML/JS + a lightweight local backend, or a single-file app) is fine — pick something a developer can stand up quickly, and state your choice explicitly in the spec so the developer isn't left guessing.

When done, HANDOFF_A.md should be the ONLY thing a developer needs to read to build this correctly.
